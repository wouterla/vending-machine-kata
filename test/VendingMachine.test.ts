import "jest";
import VendingMachine from "../src/VendingMachine";

describe("VendingTest", () => {
    it("Does something", () => {
        const vendingMachine = new VendingMachine();
        expect(true).toBe(false);
    });

});
